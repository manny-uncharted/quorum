{
  "name": "@veridex/quorum",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous multi-agent prediction desk on DeepBook Predict (Sui Overflow 2026)",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "markets": "bun run src/scripts/markets.ts",
    "preview": "bun run src/scripts/preview.ts",
    "desk": "bun run src/scripts/desk.ts",
    "settle": "bun run src/scripts/settle.ts",
    "loop": "bun run src/scripts/loop.ts",
    "serve": "bun run src/scripts/serve.ts",
    "spike": "bun run src/scripts/spike.ts",
    "oracle": "bun run src/scripts/oracle.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "^1.45.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.3.3"
  }
}
